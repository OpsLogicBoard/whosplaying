# Expo / React Native + Metro need a flat node_modules to resolve transitive
# deps (@babel/runtime, @expo/vector-icons, react-native-css-interop) and
# Expo's runtime virtual modules (expo/virtual/env). pnpm's default isolated
# layout breaks these. Hoisted linking is the Expo-recommended monorepo setting.
node-linker=hoisted
