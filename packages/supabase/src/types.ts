// PLACEHOLDER — regenerate with `pnpm --filter @whosplaying/supabase gen-types`
// after a successful `supabase db reset`. Until then, queries fall back to `any`.

export type Json = string | number | boolean | null | { [k: string]: Json } | Json[]

export type Database = any
